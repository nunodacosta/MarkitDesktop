/*
 Classe para reiniciar o sistema. Numa primeira fase será util para actualisações
e configurações do sistema com ficheiros classe.
Numa fase posterior poderá (ou não ) fazer a compilação recolocando os ficheiros
no devido package.
Requer uso de biblioteca que funciona do seguinte modo:
quando for chamada a funlção de restart a biblioteca é iniciada e vai fechar a
aplicação, para fazer o seu arranque logo de seguida. A aplicação vai encerrar 
a biblioteca.

Duas abordagens possiveis: no arranque a aplicação verifica se a biblioteca está em funcionamento
e encerra-a. Ou deve ser desenvolvido sistema de gestão para saber se a aplicação 
está a arrancar pela primeira vez na sessão ou se é um reinicio
 */
package system;

/**
 *
 * @author pd-sta-mkt
 */
public class Restart {
    
}
